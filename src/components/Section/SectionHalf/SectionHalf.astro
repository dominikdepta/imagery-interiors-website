---
interface Props {
  class?: string;
  fullHeight?: boolean;
}

const { class: className, fullHeight } = Astro.props;
---

<section
  class:list={[
    "flex flex-col lg:flex-row",
    fullHeight && "lg:[@media(max-height:1000px)]:min-h-svh",
    className,
  ]}
>
  <slot />
</section>
