---
// import { Image } from "astro:assets";
import heroImage from "./hero-interior.webp";
import Button from "../Button/Button.astro";
import Heading from "../Heading/Heading.astro";
import SectionHalf from "../Section/SectionHalf/SectionHalf.astro";
import SectionHalfContent from "../Section/SectionHalf/SectionHalfContent.astro";
import SectionHalfImage from "../Section/SectionHalf/SectionHalfImage.astro";
---

<SectionHalf>
  <SectionHalfContent class="bg-blue">
    <Heading level="1" class="lg:mt-64">
      We create spaces that <span class="italic tracking-tight">inspire</span>
      and fill with
      <span class="italic tracking-tight">positive</span> energy.
    </Heading>

    <p class="mt-4 md:mt-6 text-xs md:text-lg leading-snug">
      Imagery Interiors is more than just design. We care about individuality
      and enhancing the lifestyle of our clients.
    </p>

    <Button
      type="link"
      variant="underlined"
      class="inline-block mt-6 md:mt-14"
      href="/"
    >
      Read more
    </Button>
  </SectionHalfContent>

  <SectionHalfImage alt="Interior design" src={heroImage} />
</SectionHalf>
