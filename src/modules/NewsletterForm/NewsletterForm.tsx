export interface NewsletterFormProps {
  className?: string;
}

export const NewsletterForm = ({ className }: NewsletterFormProps) => {
  return <form className={className}>[NewsletterForm]</form>;
};
